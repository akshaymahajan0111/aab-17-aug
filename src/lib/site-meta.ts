export interface SiteMeta {
	name: string;
	summary: string;
}

export const siteMeta: SiteMeta = {
	name: "Hello World",
	summary: "A quiet, welcoming place to arrive, take a breath, and begin again.",
};
